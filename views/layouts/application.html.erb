<%
  def link_to(name,url)
    return "<a href = '#{url}'>#{name}</a>"
  end


%>

<html>
  <body>
    <h1>You're using a layout</h1>
    <p>
      <h3> I'm a header</h3>
      <p>Put things in the header</p>
      <a href='me'>Me</a>
      <%= link_to("Twitter", "http://twitter.com")%>
      <%= link_to("Shannon", "shannon")%>

<%= @request.query.inspect %>

<form method='post' action="<%= @request.path %>">
    First name: <input type="text" name="first_name" placeholder='first name'/><br/>
    Gender: <input type="radio" name="gender" value="male">Male <input type="radio" name="gender" value="female">Female<br/>
    <input type="submit" value="Submit" />
</form>
<% if @request.query['first_name'] && !@request.query['first_name'].empty? %>
  <%="Hey #{@request.query['first_name']}, you are #{@request.query['gender']}!" %>
  <%else%>
  Enter your name so we can greet you!
  <% end %>
    </p>
    <hr />
    <div>
      <%= yield %>
    </div>
    <hr />
    <div>
      <h3> I'm a footer</h3>
      <p>Put things in the footer</p>
    </div>
  </body>
</html>